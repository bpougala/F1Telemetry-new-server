# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Query {
  meetings: [Meeting]!
  sessions(meeting_keys: [Int]!): [Session]!
  drivers(session_key: Int!): [Driver]!
  positions(session_key: Int!): [Position]!
  raceControls(session_key: String!): [RaceControl]!
  carData(session_key: String!, driver_number: Int!): [CarData]!
  laps(session_key: String!, driver_number: Int): [Lap]!
}

type Session {
  session_key: Int!
  session_name: String!
  date_start: String!
  date_end: String!
  gmt_offset: String!
  session_type: String!
  meeting_key: Int!
  location: String!
  country_key: Int!
  country_code: String!
  country_name: String!
  circuit_key: Int!
  circuit_short_name: String!
  year: Int!
}

type Meeting {
  meeting_name: String!
  meeting_official_name: String!
  location: String!
  country_key: Int!
  country_code: String!
  country_name: String!
  circuit_key: Int!
  circuit_short_name: String!
  date_start: String!
  gmt_offset: String!
  meeting_key: Int!
  year: Int!
}

type Driver {
  session_key:   Int!
  meeting_key:    Int!
  broadcast_name: String!
  country_code:   String!
  first_name: String!
  full_name: String!
  headshot_url:   String!
  last_name:      String!
  driver_number: Int!
  team_colour: String
  team_name: String
  name_acronym: String
}

type Position {
  session_key: Int!
  meeting_key: Int!
  driver_number: Int!
  date:          String!
  position: Int!
}

type RaceControl {
  session_key: Int!
  meeting_key: Int!
  date: String!
  category: String
  flag: String
  lap_number: Int
  message: String
  driver_number: Int
  scope: String
  sector: Int
}

type CarData {
  brake: Int!
  date: String!
  driver_number: Int!
  drs: Int!
  meeting_key: Int!
  n_gear: Int!
  rpm: Int!
  session_key: Int!
  speed: Int!
  throttle: Int!
}

type Lap {
  meeting_key: Int!
  session_key: Int!
  driver_number: Int!
  i1_speed: Int
  i2_speed: Int
  st_speed: Int
  date_start: String!
  lap_duration: Float
  is_pit_out_lap: Boolean!
  duration_sector_1: Float
  duration_sector_2: Float
  duration_sector_3: Float
  segments_sector_1: [Int]
  segments_sector_2: [Int]
  segments_sector_3: [Int]
  lap_number: Int!
}